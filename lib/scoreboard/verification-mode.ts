// Temporary launch mode: reps can only see their own detail data; everyone else's
// name and hover blurb are masked so they still get a sense of where they rank
// without seeing anyone else's real data. Managers always see everything. Flip
// this to false once the data has been verified as accurate.
export const VERIFICATION_MODE_ACTIVE = true;
